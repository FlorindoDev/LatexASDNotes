int* p1 = new int{99};

int* p2 = p1; // poteziali proplemi, poiche se la locazione viene 
            //deallocata abbiamo due puntatori a un locazione che 
            // non esiste creando problemi
            
delete p1; // now p2 doesn't point to a valid object

p1 = nullptr; // Diamo un valore nullo per sicurezza

char* p3 = new char{'x'}; // p3 ora punta alla memoria puntata da p2 

*p2 = 999; // Potrebbe causare problemi

cout << *p3 << '\n'; // non stampa x