double read_and_sum(int s){
    // legge s interi da cin e restituisce la somma
    // s positivo
    Vector v(s);
    for(int i=0; i<s; i++)
        cin >> v[i];
        
    double sum=0;
    for(int i=0; i<s; i++)
        sum+=v[i];
        
    return sum;
}